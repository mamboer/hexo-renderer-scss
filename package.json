{
  "name": "hexo-renderer-scss",
  "version": "0.0.0-development",
  "description": "Yet Another Sass renderer plugin for Hexo",
  "main": "index",
  "repository": {
    "type": "git",
    "url": "git@github.com:mamboer/hexo-renderer-scss.git"
  },
  "keywords": [
    "hexo",
    "sass",
    "scss",
    "renderer"
  ],
  "author": {
    "name": "mamboer",
    "email": "mamboer@gmail.com"
  },
  "license": "MIT",
  "engines": {
    "hexo": ">= 3.0.0"
  },
  "dependencies": {
    "node-sass": "^7.0.0",
    "node-sass-magic-importer": "^5.1.1"
  },
  "bugs": {
    "url": "https://github.com/mamboer/hexo-renderer-scss/issues"
  },
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 0",
    "semantic-release": "semantic-release",
    "semantic-release-test": "semantic-release --dry-run --no-ci -e ./release-local.config.js",
    "semantic-release-local": "semantic-release --no-ci -e ./release-local.config.js",
    "travis-deploy-once": "travis-deploy-once"
  },
  "devDependencies": {
    "semantic-release": "^21.0.1",
    "travis-deploy-once": "^4.4.1"
  }
}
